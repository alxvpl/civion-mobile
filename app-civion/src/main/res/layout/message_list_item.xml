<?xml version="1.0" encoding="utf-8"?>
<!--
  CIVION Mail override of the message list row.

  A resource of this name in the application module wins over the one in legacy/ui/legacy,
  so the row is changed without touching upstream code. The row that is inflated, the view
  ids, the view types and the binding in MessageViewHolder are all unchanged.

  The row is three columns:

    contact picture | subject / sender / preview | star over date (and paperclip)

    - The contact picture (and the check mark that replaces it in selection) is centred on the
      row by a constraint bias between the row's own guidelines, not by a top margin, so it
      sits the same way beside a two-line row and a four-line one.

    - The text block is three lines: the subject, the sender, the preview - each one line, each
      cut with an ellipsis. The engine hands the row "sender - preview" as one string; the
      preview view (SenderLinePreviewTextView, app-civion) puts the sender on its own line. The
      block runs from the picture to the trailing column on every line, so no text sits under
      the metadata.

    - The trailing column: the paperclip, as a 20dp chip in the row's corner when there is one
      (a 14dp glyph, so it reads as a mark and not as a speck), then the star at the same 20dp,
      then the date. A barrier on the column's start is what the text ends at, so the column's
      width - the date's, never less than 52dp - is the only thing that decides where the text
      stops; a long subject cannot push the date, and a paperclip cannot move the star. The star's tap
      area is 48dp around it.

    - `account_color_chip` and `status` are aligned to `subject`, so the chip and the
      reply/forward marker stay on the subject line.

    - The row's horizontal padding, the picture keyline and the vertical padding come from
      app-civion's values/dimensions.xml.

  Deliberately not changed: text sizes and text appearances, the contact picture size, and the
  row density modes.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:foreground="?attr/selectableItemBackground"
    >

    <Space
        android:id="@+id/margin_start"
        android:layout_width="@dimen/messageListHorizontalPadding"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="parent"
        />

    <ImageView
        android:id="@+id/selected"
        android:layout_width="@dimen/messageListContactPictureSize"
        android:layout_height="@dimen/messageListContactPictureSize"
        android:layout_marginStart="@dimen/messageListHorizontalPadding"
        android:contentDescription="@null"
        android:background="@drawable/message_list_item_selection_background"
        android:padding="4dp"
        android:visibility="gone"
        app:tint="?attr/colorOnSecondaryContainer"
        app:layout_constraintBottom_toTopOf="@+id/bottom_guideline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/top_guideline"
        app:layout_constraintVertical_bias="0.5"
        app:srcCompat="@drawable/ic_check"
        />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/contact_picture"
        android:layout_width="@dimen/messageListContactPictureSize"
        android:layout_height="@dimen/messageListContactPictureSize"
        android:layout_marginStart="@dimen/messageListHorizontalPadding"
        android:background="@android:color/transparent"
        app:layout_constraintBottom_toTopOf="@+id/bottom_guideline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/top_guideline"
        app:layout_constraintVertical_bias="0.5"
        tools:src="@drawable/ic_person"
        />

    <nl.civion.mobile.list.SenderLinePreviewTextView
        android:id="@+id/preview"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/messageListDefaultTextViewMargin"
        android:layout_marginEnd="8dp"
        android:bufferType="spannable"
        android:ellipsize="end"
        android:lineSpacingMultiplier="@dimen/messageListDefaultLineSpacingMultiplier"
        android:singleLine="false"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        app:layout_constraintBottom_toBottomOf="@+id/bottom_guideline"
        app:layout_constraintEnd_toStartOf="@+id/barrier_trailing_start"
        app:layout_constraintStart_toEndOf="@+id/barrier_start"
        app:layout_constraintTop_toTopOf="@+id/barrier_first_line_bottom"
        app:layout_constraintVertical_bias="0.0"
        tools:maxLines="2"
        tools:text="Sender – Message preview text that can span multiple lines. Users can specify in settings how much of that is displayed."
        />

    <ImageView
        android:id="@+id/account_color_chip"
        android:layout_width="7dp"
        android:layout_height="12dp"
        android:paddingStart="0dp"
        android:paddingEnd="4dp"
        android:contentDescription="@null"
        app:layout_constraintBottom_toBottomOf="@+id/subject"
        app:layout_constraintStart_toEndOf="@+id/barrier_start"
        app:layout_constraintTop_toTopOf="@+id/subject"
        app:srcCompat="@drawable/ic_account_color"
        tools:tint="@color/material_blue_700"
        />

    <ImageView
        android:id="@+id/status"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:paddingEnd="2dp"
        app:layout_constraintStart_toEndOf="@+id/account_color_chip"
        app:layout_constraintTop_toTopOf="@+id/subject"
        app:layout_constraintBottom_toBottomOf="@+id/subject"
        app:srcCompat="@drawable/ic_reply"
        tools:ignore="RtlSymmetry"
        />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/subject"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        app:layout_constrainedWidth="true"
        app:layout_constraintEnd_toStartOf="@+id/thread_count"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toEndOf="@+id/status"
        app:layout_constraintTop_toTopOf="@+id/top_guideline"
        tools:text="Subject"
        />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/thread_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:focusable="false"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="?attr/colorOnSurfaceVariant"
        app:layout_constraintBaseline_toBaselineOf="@+id/subject"
        app:layout_constraintEnd_toStartOf="@+id/barrier_trailing_start"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@+id/subject"
        app:layout_goneMarginEnd="8dp"
        tools:text="3"
        />

    <ImageView
        android:id="@+id/attachment"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="@dimen/messageListHorizontalPadding"
        android:background="@drawable/message_list_attachment_chip"
        android:padding="3dp"
        android:scaleType="fitCenter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_attachment"
        />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/messageListHorizontalPadding"
        android:minWidth="52dp"
        android:gravity="end"
        android:singleLine="true"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:layout_marginTop="2dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/star"
        tools:text="Oct 27"
        />

    <ImageView
        android:id="@+id/star"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="@dimen/messageListHorizontalPadding"
        android:layout_marginTop="2dp"
        android:src="@drawable/btn_select_star"
        android:contentDescription="@string/star_button_description"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/attachment"
        app:layout_goneMarginTop="@dimen/messageListDefaultVerticalPadding"
        />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/start_keyline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_begin="72dp"
        />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/top_guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_begin="@dimen/messageListDefaultVerticalPadding"
        />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/bottom_guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_end="@dimen/messageListDefaultVerticalPadding"
        />

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrier_first_line_bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="bottom"
        app:constraint_referenced_ids="subject,thread_count,status,account_color_chip"
        />

    <!-- The trailing column: date above, star (and paperclip beside it) below. Text ends here. -->
    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrier_trailing_start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="start"
        app:constraint_referenced_ids="date,star,attachment"
        />

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrier_start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="end"
        app:constraint_referenced_ids="margin_start,contact_picture_click_area"
        />

    <View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="?android:attr/listDivider"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/contact_picture_click_area"
        />

    <View
        android:id="@+id/star_click_area"
        android:layout_width="48dp"
        android:layout_height="48dp"
        app:layout_constraintBottom_toBottomOf="@+id/star"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/star"
        />

    <View
        android:id="@+id/contact_picture_click_area"
        android:layout_width="@dimen/messageListStartKeyline"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
